import { defineConfig } from "astro/config";

// TODO: set `site` to your custom domain once configured, e.g. "https://example.com".
// With a custom domain the site is served from root, so `base` stays unset.
export default defineConfig({
  site: "https://kaenirr.com",
});
